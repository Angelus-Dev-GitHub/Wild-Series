{% extends "base.html.twig" %}

{% block body %}
<h1>{{ bienvenue }} sur Wild Series</h1>
    <div></div>
    <a href="{{ path("program_index") }}">Accéder aux programmes</a>
    </div>
    <div>
    <a href="{{ path("program_new") }}">Ajouter un nouveau programme</a>
    </div>

{% endblock %}