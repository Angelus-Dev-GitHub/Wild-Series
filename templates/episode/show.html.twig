{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h2>Episode</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ episode.title }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ episode.number }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ episode.synopsis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('episode_index') }}">back to list</a>

    <a href="{{ path('episode_edit', {'slug': episode.slug}) }}">edit</a>

    {{ include('episode/_delete_form.html.twig') }}

    <a href="{{ path('comment_show', {'id': episode.id}) }}">Voir les commentaires</a>
    <a href="{{ path('comment_new', {'episodeSlug': episode.slug}) }}">Ajouter un commentaire</a>

{% endblock %}
