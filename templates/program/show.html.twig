{# templates/program/show.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}

    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>
            {% for season in seasons %}
                <li>
                    <h5>saison {{ season.number }} - {{ season.year }} - Description : {{ season.description }}</h5>
                    <a href="{{ path("program_season_show", { "program_id": program.id, "season_number": season.number }) }}">Voir la saison</a>
                </li>
            {% endfor %}


        </div>
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>

{% endblock %}


{# Season Show
{% extends 'base.html.twig' %}

{% block title %}All Seasons{% endblock %}

{% block body %}
    <h1>Toutes les saisons de la Série {{ program.title }}</h1>
    <div>
        {% for episode in episodes %}
            <li>
                <h5>{{ episode.number }} - {{ episode.title }} - Synopsis : {{ episode.synopsis }}</h5>
                <a href="{{ path("program_season_episode", { "program_id": program.id, "season_number": season.number, "episode_number": episode.number }) }}">Voir l'épisode</a>
            </li>
        {% endfor %}
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des séries
    </a>
{% endblock %}
    #}

{# show episodes
{% extends 'base.html.twig' %}

{% block title %}All Episodes{% endblock %}

{% block body %}
    <h1>Série {{ program.title }}</h1>
    <h2>Saison {{ season.number }}</h2>
    <h3>Episode: {{ episode.title }}</h3>
    <div>
       <p>{{ episode.synopsis }}</p>
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des séries
    </a>
{% endblock %}
#}